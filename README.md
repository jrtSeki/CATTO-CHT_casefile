1. install Openfoam v5
2. wmake the CATTO-CHT solver
3. use command in terminal to run caculation<br>
   ```bash
   bash run.sh
   ```
5. use command in terminal to run Post-processing<br>
   ```bash
   bash post.sh
   ```
